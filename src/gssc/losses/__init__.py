"""Loss functions: Lovász-Softmax (`lovasz`).

The KL posterior, auxiliary-head and focal cross-entropy terms are not here -- they
are implemented inside `gssc.diffusion.multinomial` alongside the diffusion they
belong to.
"""
